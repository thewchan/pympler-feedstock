{% set name = "pympler" %}
{% set version = "0.7" %}
{% set sha256 = "e8f8d3d2bada9a64948adfa118eca29222af2546a28ba480c13bce21aae8a984" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/{{ name }}/{{ name }}/archive/{{ version }}.tar.gz
#  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - pip
    - python
    - setuptools
  run:
    - python

test:
  source_files:
    - test/*
  commands:
    - python test/runtest.py

about:
  home: https://github.com/pympler/pympler
  license: Apache-2
  license_family: Apache
  license_file: LICENSE
  summary: 'Development tool to measure, monitor and analyze the memory
  behavior of Python objects in a running Python application. '
  description: |
    A development tool to measure, monitor and analyze the memory
    behavior of Python objects.
  doc_url: http://pythonhosted.org/Pympler/
  dev_url: https://github.com/pympler/pympler

extra:
  recipe-maintainers:
    - CJ-Wright
