{% set name = "pympler" %}
{% set version = "0.6" %}
{% set sha256 = "e22d2d1555739d2d46b2e51f941fa4e6c5142fbe73bd04132665e595b05cab7b" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/{{ name }}/{{ name }}/archive/{{ version }}.tar.gz
#  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - pip
    - python
    - setuptools
  run:
    - python

test:
  source_files:
    - test/*
  commands:
    - python test/runtest.py

about:
  home: https://github.com/pympler/pympler
  license: Apache-2
  license_family: Apache
  license_file: LICENSE
  summary: 'Development tool to measure, monitor and analyze the memory
  behavior of Python objects in a running Python application. '
  description: |
    A development tool to measure, monitor and analyze the memory
    behavior of Python objects.
  doc_url: http://pythonhosted.org/Pympler/
  dev_url: https://github.com/pympler/pympler

extra:
  recipe-maintainers:
    - CJ-Wright
